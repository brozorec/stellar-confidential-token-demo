{
  "name": "@ctd/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Goldsky turbo pipeline + Cloudflare Worker read API for confidential-token events (durable, full-history complement to the RPC's ~7-day window).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.12",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.79.0"
  }
}
